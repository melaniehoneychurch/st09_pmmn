{% extends "base.html.twig" %}

{% block title product.frenchName %}


{% block body %}

	{%if product.securityFormNameFile%}
      	<a href="{{ path('product.show.securityForm', {slug: product.slug, id: product.id}) }}" class="mt-2 d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm"><i class="fas fa-file-pdf fa-sm text-white-50"></i> {% trans %}Display the complete safety data sheet{% endtrans %}</a>
		<a href="{{ vich_uploader_asset(product, 'securityFormFile') }}" class="mt-2 d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm" download="{{product.frenchName}}_fiche_securite"><i class="fas fa-download fa-sm text-white-50"></i> {% trans %}Download the complete safety data sheet{% endtrans %}</a>
	{% else %}
		<a href="" class="mt-2 d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm disabled"><i class="fas fa-times fa-sm text-white-50"></i> {% trans %}No safety data sheet{% endtrans %}</a>
	{%endif%}
	<a  href="" class="printSummary mt-2 d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm"><i class="fas fa-print fa-sm text-white-50"></i> {% trans %}Print the summary safety sheet{% endtrans %}</a>
	<a href="" class="printQrCode mt-2 d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm"><i class="fas fa-print fa-sm text-white-50"></i> {% trans %}Print product QR Code{% endtrans %}</a>

	{% include "/product/summarySheet.html.twig" %}
	
{% endblock %}

{% block javascripts %}
{{ parent() }}

{{ encore_entry_script_tags('printDiv') }}


{% endblock %}


