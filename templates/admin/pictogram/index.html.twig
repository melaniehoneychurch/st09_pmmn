{% extends "base.html.twig" %}

{% block title 'Gérer Produit' %}

{% block body %}
	{% include 'admin/navBarAdmin.html.twig' %}

	<div class="container mt-4">

		<h1>Gérer les pictogrammes</h1>

		{% include 'admin/adminFlashes.html.twig' %}

		<table class="table table-striped">
			<thead>
				<tr>
					<th>Titre</th>
					<th>Action</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				{% for pictogram in pictograms %}
					<tr>
						<td>{{ pictogram.name }}</td>
						<td>
							<a href="{{ path('admin.pictogram.edit', {id: pictogram.id}) }}" class="btn btn-secondary">Editer</a>
							<form method="post" action="{{ path('admin.pictogram.delete', {id: pictogram.id}) }}" style="display: inline-block">
								<input type="hidden" name="_method" value="DELETE">
								<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pictogram.id) }}">
								<button class="btn btn-danger">Supprimer</button>
							</form>
						</td>
					</tr>

				{% endfor %}
			</tbody>
		</table>

		<div class="text-right">
			<a href="{{ path('admin.pictogram.new') }}" class="btn btn-primary">Créer un nouveau pictogram</a>
		</div>
	</div>
{% endblock %}
