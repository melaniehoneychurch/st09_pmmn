{% extends "base.html.twig" %}

{% block title 'Editer mot de passe' %}

{% block body %}

	{{ form_start(form) }}

	{# Form #}
	<!-- Content Row -->
	<div class="row">

		<!-- Area Chart -->
		<div class="col-xl-12 col-lg-12">
			<div class="card shadow mb-4">
				<!-- Card Header -->
				<div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
					<h6 class="m-0 font-weight-bold text-primary">Modifier mot de passe</h6>
				</div>

				<!-- Card Body - search options -->
				<div class="card-body">
					<div class="row">
						<div class="col-md-6">
							{{ form_row(form.password) }}
						</div>
					</div>
				</div>

			</div>

			<div class="text-right">
				<div style="display:inline-block;"><button class="btn btn-sm btn-primary shadow-sm">{{ button|default('Enregistrer') }}</button></div>
				<div style="display:inline-block"><a href="{{ path('user.cancel') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">Annuler</a></div>
			</div>

		</div>

	</div>

	{{ form_end(form) }}

{% endblock %}
