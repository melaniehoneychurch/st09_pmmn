{{ form_start(form) }}

{# Form #}
<!-- Content Row -->
<div class="row">

    <!-- Area Chart -->
    <div class="col-xl-12 col-lg-12">
        <div class="card shadow mb-4">

            <!-- Card Header -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">{{ button|default('Formulaire') }} un pictogramme</h6>
            </div>

            <!-- Card Body - search options -->
            <div class="card-body">
                {{ form_row(form.imageFile, {'label':"Déposez une image du pictogramme"}, {'attr': {'onchange': "document.getElementById('imageFile').src = window.URL.createObjectURL(this.files[0])"}}) }}
                <img id="imageFile" alt="pictogramme" width="100" height="100" />

                {{ form_row(form.type, {'label':"Indiquez le type de pictogramme"}) }}
                {{ form_row(form.name, {'label':"Entrez le nom du pictogramme"}) }}
            </div>

        </div>

        <div class="text-right">
            <div style="display:inline-block"><button class="btn btn-primary">{{ button|default('Enregistrer') }}</button></div>
            <div style="display:inline-block"><a href="{{ path('productmanager.pictogram.cancel') }}" class="mt-2 d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm"> Annuler</a>
</div>
        </div>

    </div>

</div>

{{ form_end(form) }}
