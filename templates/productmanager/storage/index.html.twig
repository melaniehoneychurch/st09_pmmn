{% extends "base.html.twig" %}

{% block title 'Gérer Stockages' %}

{% block body %}

	{% include 'admin/adminFlashes.html.twig' %}

	<h2>Gérer les stockages</h2>

	{#	#}{# Recherche #}
	{#	<!-- Content Row -->#}
	{#	<div class="row">#}

	{#		<!-- Area Chart -->#}
	{#		<div class="col-xl-12 col-lg-12">#}
	{#			<div class="card shadow mb-4">#}

	{#				<!-- Card Header -->#}
	{#				<div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">#}
	{#					<h6 class="m-0 font-weight-bold text-primary">Recherche</h6>#}
	{#				</div>#}

	{#				<!-- Card Body - search options -->#}
	{#				<div class="card-body">#}

	{# TODO : recherche picto #}

	{#				</div>#}

	{#			</div>#}
	{#		</div>#}

	{#	</div>#}

	{# Table #}
	<!-- Content Row -->
	<div class="row">

		<!-- Area Chart -->
		<div class="col-xl-12 col-lg-12">
			<div class="card shadow mb-4">

				<!-- Card Header -->
				<div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
					<h6 class="m-0 font-weight-bold text-primary">Liste</h6>
				</div>

				<!-- Card Body - search options -->
				<div class="card-body">

					<table class="table table-striped">
						<thead>
						<tr>
							<th>Titre</th>
							<th>Action</th>
							<th></th>
						</tr>
						</thead>
						<tbody>
						{% for storage in storages %}
							<tr>
								<td>{{ storage.name }}</td>
								<td>
									<a href="{{ path('productmanager.storage.edit', {id: storage.id}) }}" class="btn btn-secondary">Editer</a>
									<form method="post" action="{{ path('productmanager.storage.delete', {id: storage.id}) }}" style="display: inline-block">
										<input type="hidden" name="_method" value="DELETE">
										<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ storage.id) }}">
										<button class="btn btn-danger">Supprimer</button>
									</form>
								</td>
							</tr>

						{% endfor %}
						</tbody>
					</table>

					{# display navigation #}
					<div class="navigation">
						{{ knp_pagination_render(storages) }}
					</div>

				</div>

			</div>

			<div class="text-right">
				<div style="display:inline-block">
					<a href="{{ path('productmanager.storage.new') }}" class="btn btn-primary">Créer un nouveau stockage</a>
				</div>
			</div>

		</div>

	</div>

{% endblock %}
